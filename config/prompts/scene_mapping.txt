You are building a scene map for an automated movie recap video.

The recap narration is split into {scene_count} scenes (one voiceover beat each).
The subtitle transcript uses line numbers like: INDEX|TIMESTAMP|TEXT

For EACH scene, pick which subtitle lines best match the VISUAL moment being described in the narration.
Use the subtitle index numbers (left column), not timestamps.

Narration scenes:
{narration_scenes}
{subtitle_hint}
Subtitle index (sample — full transcript is in the notebook):
{subtitle_index_sample}

Reply with ONLY valid JSON (no markdown):
{{
  "scenes": [
    {{
      "scene_id": 1,
      "subtitle_start": 1,
      "subtitle_end": 8,
      "music_mood": "mystery|tense|action|reveal|emotional|calm",
      "reason": "brief note"
    }}
  ]
}}

Rules:
- Exactly {scene_count} scenes, scene_id {scene_id_start}..{scene_id_end}
- subtitle_start and subtitle_end are inclusive 1-based line numbers from the SRT index
- Valid subtitle lines are 1 through {max_subtitle_line} only — never exceed {max_subtitle_line}
- Each range should cover 3–40 subtitle lines; prefer the visual beat, not the whole film
- Recap narration is NOT in film order — pick the subtitle lines that match each scene's visuals even if line numbers go backward
- Different scenes may reuse similar moments; never shift a scene's lines forward just because an earlier scene used a later timestamp
- Clip source will be at most {max_clip_sec} seconds — pick tight visual moments
- music_mood must match narration energy: calm (exposition), mystery (questions), tense (danger), action (chase/fight), reveal (twist), emotional (loss/love)
