Design ONE YouTube thumbnail for this movie recap.

Movie: {topic}
LOCKED video title: {title}
Thumbnail hook text (from hook package — use for overlay): {thumbnail_text}

STYLE BRIEF (match competitor thumbnail patterns):
{style_notes}

We fetch a real photo from Wikimedia Commons (not AI). Your job: specify search + confirm overlay.

Reply with ONLY raw JSON:
{{
  "image_search_query": "Wikipedia/Wikimedia search: lead actor + film title + year (e.g. 'Inception 2010 film' or 'Leonardo DiCaprio Inception')",
  "fallback_search_query": "second search if first fails",
  "overlay_title": "2–4 words MAX — use thumbnail_text from hook package",
  "overlay_subtitle": "RECAP or EXPLAINED or ENDING — one word",
  "text_color": "#FFFFFF",
  "accent_color": "#E63946",
  "style_notes": "hero face right-weighted, dark left third for text, bold sans-serif white + stroke, last word red accent"
}}

Rules:
- High contrast, readable on mobile at 120px width
- One hero face or iconic still — not a 6-panel collage
- No studio logos, no rating badges, no text in the source image
- Overlay text must match title + cold open promise
- Curiosity without lying (no fake red arrows on things that never happen)
